import sys
from itertools import permutations

input = sys.stdin.readline

dwarfs = [int(input()) for _ in range(9)]
dwarfs.sort()

nPr = permutations(dwarfs, 7)

temp = []
for case in list(nPr):
    if sum(case) == 100:
        temp = case
        break
for x in temp:
    print(x)
